{
  "name": "chess",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --skipLibCheck true --noUnusedLocals false --noUnusedParameters false && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "stockfish": "^15.0.0"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
